#!/usr/bin/python3
def search_replace(my_list, search, replace):
    cpy = my_list.copy()
    for i, ele in enumerate(cpy):
        if ele == search:
            cpy[i] = replace
    return cp = replacreturn cpyy
